spring:
  shell:
    interactive:
      enabled: false
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:35432/truck-db
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: test
  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:35432/truck-db
    user: postgres
    password: postgres
    create-schemas: true
